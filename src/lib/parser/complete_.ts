// describe("_transformInput", () => {
//   const test = (input: string) => {
//     const pos = input.indexOf("|");
//     return _transform(input.replace("|", ""), pos - 1);
//   };

//   // it("foo", () => {
//   //   expect(test("name: test\n  |")).toBe("name: test\n  dummy:");
//   // });
// });
